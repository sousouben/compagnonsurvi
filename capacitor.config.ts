import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.compagnondesurvie.app',
  appName: 'compagnon-de-survie',
  webDir: 'dist'
};

export default config;
